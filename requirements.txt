Django==6.1
openpyxl==3.1.5
gunicorn==23.0.0
requests==2.34.2
whitenoise==6.12.0
python-barcode==0.16.1
Pillow==11.0.0
cryptography==50.0.1

# Windows has no fork(), so gunicorn cannot run there at all. waitress is a pure-Python
# WSGI server that runs anywhere. The environment marker means a Windows install gets it
# automatically while Linux/macOS/Docker keep using gunicorn and never download it.
# Run it with:  waitress-serve --listen=0.0.0.0:3200 config.wsgi:application
waitress==3.0.2; sys_platform == "win32"
